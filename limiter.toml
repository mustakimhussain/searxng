[real_ip]
# Trust Render's reverse proxy headers for accurate client IP identification
x_for = 1

[botdetection.ip_limit]
# Link requests per time window
link_token = true
filter = true

[botdetection.ip_limit.search]
# Maximum requests allowed per IP per time window
capacity = 30
ip_limit_time = 60
